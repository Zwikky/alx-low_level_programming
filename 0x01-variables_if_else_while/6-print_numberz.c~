#include <stdio.h>

/**
 * main - MAin Function --- prints all single digit numbers of base 10 starting from 0
 *
 * Return: just a 0
 */
int main(void)
{
	int i;

	for (i = 0; i <= 9; i++)
		putchar(i);
	putchar('\n');

	return (0);
}
